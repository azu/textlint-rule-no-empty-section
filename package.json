{
  "directories": {
    "test": "test"
  },
  "author": "azu",
  "license": "MIT",
  "files": [
    "bin/",
    "lib/",
    "src/"
  ],
  "name": "textlint-rule-no-empty-section",
  "version": "1.0.1",
  "description": "textlint rule not allow to create empty section.",
  "main": "lib/textlint-rule-no-empty-section.js",
  "scripts": {
    "test": "mocha test/",
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build"
  },
  "keywords": [
    "textlint",
    "rule"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/azu/textlint-rule-no-empty-section.git"
  },
  "bugs": {
    "url": "https://github.com/azu/textlint-rule-no-empty-section/issues"
  },
  "homepage": "https://github.com/azu/textlint-rule-no-empty-section",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-jsdoc-to-assert": "^4.0.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.16.3",
    "mocha": "^3.1.0",
    "power-assert": "^1.4.1",
    "textlint-tester": "^2.0.0"
  },
  "dependencies": {
    "select-section": "^0.4.0"
  }
}
